# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(joint_dispatcherTaskLib)
ADD_LIBRARY(${JOINT_DISPATCHER_TASKLIB_NAME} SHARED 
    ${JOINT_DISPATCHER_TASKLIB_SOURCES})
add_dependencies(${JOINT_DISPATCHER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${JOINT_DISPATCHER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${JOINT_DISPATCHER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${JOINT_DISPATCHER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${JOINT_DISPATCHER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${JOINT_DISPATCHER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${JOINT_DISPATCHER_TASKLIB_HEADERS}
    DESTINATION include/orocos/joint_dispatcher)

