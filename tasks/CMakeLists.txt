# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(dispatcher_baseTaskLib)
ADD_LIBRARY(${DISPATCHER_BASE_TASKLIB_NAME} SHARED 
    ${DISPATCHER_BASE_TASKLIB_SOURCES})
add_dependencies(${DISPATCHER_BASE_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${DISPATCHER_BASE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${DISPATCHER_BASE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${DISPATCHER_BASE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${DISPATCHER_BASE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${DISPATCHER_BASE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${DISPATCHER_BASE_TASKLIB_HEADERS}
    DESTINATION include/orocos/dispatcher_base)

